/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesCreateResourceCustomAccessLevel } from "../../funcs/resourcesCreateResourceCustomAccessLevel.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateResourceCustomAccessLevelRequest$inboundSchema,
};

export const tool$resourcesCreateResourceCustomAccessLevel: ToolDefinition<
  typeof args
> = {
  name: "resources-create-resource-custom-access-level",
  description:
    `Creates a custom access level on a resource. If the resource is a parent type, the role is created on all child resources.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resourcesCreateResourceCustomAccessLevel(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

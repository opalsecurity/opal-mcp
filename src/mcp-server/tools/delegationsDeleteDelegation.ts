/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { delegationsDeleteDelegation } from "../../funcs/delegationsDeleteDelegation.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteDelegationRequest$inboundSchema,
};

export const tool$delegationsDeleteDelegation: ToolDefinition<typeof args> = {
  name: "delegations-delete-delegation",
  description: `Deletes a delegation by its ID.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await delegationsDeleteDelegation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

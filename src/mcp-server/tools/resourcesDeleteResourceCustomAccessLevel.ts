/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesDeleteResourceCustomAccessLevel } from "../../funcs/resourcesDeleteResourceCustomAccessLevel.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteResourceCustomAccessLevelRequest$inboundSchema,
};

export const tool$resourcesDeleteResourceCustomAccessLevel: ToolDefinition<
  typeof args
> = {
  name: "resources-delete-resource-custom-access-level",
  description:
    `Deletes a custom access level identified by its remote ID. If the resource is a parent type, the deletion fans out to all child resources.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resourcesDeleteResourceCustomAccessLevel(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

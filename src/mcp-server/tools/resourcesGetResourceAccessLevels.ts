/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesGetResourceAccessLevels } from "../../funcs/resourcesGetResourceAccessLevels.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetResourceAccessLevelsRequest$inboundSchema,
};

export const tool$resourcesGetResourceAccessLevels: ToolDefinition<
  typeof args
> = {
  name: "resources-get-resource-access-levels",
  description: `Get resource access levels

Returns the list of access levels defined for the resource. Resources that only offer default (unnamed) access return an empty list.`,
  scopes: ["read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resourcesGetResourceAccessLevels(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

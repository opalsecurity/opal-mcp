/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventStreamsDeleteEventStream } from "../../funcs/eventStreamsDeleteEventStream.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteEventStreamRequest$inboundSchema,
};

export const tool$eventStreamsDeleteEventStream: ToolDefinition<typeof args> = {
  name: "event-streams-delete-event-stream",
  description: `Delete event stream

Deletes an event streaming connection.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await eventStreamsDeleteEventStream(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

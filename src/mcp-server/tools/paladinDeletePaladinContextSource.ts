/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paladinDeletePaladinContextSource } from "../../funcs/paladinDeletePaladinContextSource.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeletePaladinContextSourceRequest$inboundSchema,
};

export const tool$paladinDeletePaladinContextSource: ToolDefinition<
  typeof args
> = {
  name: "paladin-delete-paladin-context-source",
  description: `Remove a Paladin context source

Removes a context source from a Paladin.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paladinDeletePaladinContextSource(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

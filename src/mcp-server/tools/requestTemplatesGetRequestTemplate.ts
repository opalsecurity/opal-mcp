/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { requestTemplatesGetRequestTemplate } from "../../funcs/requestTemplatesGetRequestTemplate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetRequestTemplateRequest$inboundSchema,
};

export const tool$requestTemplatesGetRequestTemplate: ToolDefinition<
  typeof args
> = {
  name: "request-templates-get-request-template",
  description: `Returns a \`RequestTemplate\` object.`,
  scopes: ["read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await requestTemplatesGetRequestTemplate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

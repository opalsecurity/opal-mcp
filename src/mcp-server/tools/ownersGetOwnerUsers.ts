/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ownersGetOwnerUsers } from "../../funcs/ownersGetOwnerUsers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOwnerUsersRequest$inboundSchema,
};

export const tool$ownersGetOwnerUsers: ToolDefinition<typeof args> = {
  name: "owners-get-owner-users",
  description:
    `Gets the list of users for this owner, in escalation priority order if applicable.`,
  scopes: ["read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ownersGetOwnerUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

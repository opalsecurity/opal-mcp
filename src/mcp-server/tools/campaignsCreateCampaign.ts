/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { campaignsCreateCampaign } from "../../funcs/campaignsCreateCampaign.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateCampaignInfo$inboundSchema,
};

export const tool$campaignsCreateCampaign: ToolDefinition<typeof args> = {
  name: "campaigns-create-campaign",
  description:
    `Creates a campaign. Campaign scope only supports direct access edges:
\`configuration.query.edgeFilter.directOnly\` defaults to \`true\`, is
always stored as \`true\`, and passing \`false\` returns 400.
`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await campaignsCreateCampaign(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

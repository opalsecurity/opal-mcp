/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { idpGroupMappingsCreateIdpGroupMapping } from "../../funcs/idpGroupMappingsCreateIdpGroupMapping.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateIdpGroupMappingRequest$inboundSchema,
};

export const tool$idpGroupMappingsCreateIdpGroupMapping: ToolDefinition<
  typeof args
> = {
  name: "idp-group-mappings-create-idp-group-mapping",
  description:
    `Creates or updates an individual \`IdpGroupMapping\` object (upsert operation).

**Behavior:**
- If the mapping doesn't exist, it will be created with the provided values
- If the mapping exists, only the fields provided in the request will be updated
`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await idpGroupMappingsCreateIdpGroupMapping(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

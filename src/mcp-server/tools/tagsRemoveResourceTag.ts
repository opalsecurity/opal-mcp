/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tagsRemoveResourceTag } from "../../funcs/tagsRemoveResourceTag.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveResourceTagRequest$inboundSchema,
};

export const tool$tagsRemoveResourceTag: ToolDefinition<typeof args> = {
  name: "tags-remove-resource-tag",
  description: `Removes a tag from a resource.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tagsRemoveResourceTag(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { campaignsUpdateCampaignItemReviewers } from "../../funcs/campaignsUpdateCampaignItemReviewers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateCampaignItemReviewersRequest$inboundSchema,
};

export const tool$campaignsUpdateCampaignItemReviewers: ToolDefinition<
  typeof args
> = {
  name: "campaigns-update-campaign-item-reviewers",
  description: `Update campaign item reviewers

Atomically assigns and unassigns reviewers on campaign items in one
request. All \`campaign_item_id\`s must belong to \`{campaign_id}\`.
Existing assign pairs are ignored; missing unassign pairs are skipped.
Completed reviews (\`decided_at\` set) are never removed.
Requires settings-edit permission. Returns 400 if the campaign has ended.
`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await campaignsUpdateCampaignItemReviewers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

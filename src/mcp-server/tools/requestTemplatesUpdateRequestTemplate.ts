/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { requestTemplatesUpdateRequestTemplate } from "../../funcs/requestTemplatesUpdateRequestTemplate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.UpdateRequestTemplateInfo$inboundSchema,
};

export const tool$requestTemplatesUpdateRequestTemplate: ToolDefinition<
  typeof args
> = {
  name: "request-templates-update-request-template",
  description: `Updates a request template.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await requestTemplatesUpdateRequestTemplate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paladinCreatePaladinContextSource } from "../../funcs/paladinCreatePaladinContextSource.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreatePaladinContextSourceRequest$inboundSchema,
};

export const tool$paladinCreatePaladinContextSource: ToolDefinition<
  typeof args
> = {
  name: "paladin-create-paladin-context-source",
  description: `Add a Paladin context source

Configures a context source (a Slack channel or a document) for a Paladin to read. Idempotent, so re-adding an existing source returns it.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paladinCreatePaladinContextSource(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

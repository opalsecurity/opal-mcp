/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paladinDeletePaladin } from "../../funcs/paladinDeletePaladin.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeletePaladinRequest$inboundSchema,
};

export const tool$paladinDeletePaladin: ToolDefinition<typeof args> = {
  name: "paladin-delete-paladin",
  description: `Delete Paladin

Deletes a Paladin, removing the underlying service user.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paladinDeletePaladin(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

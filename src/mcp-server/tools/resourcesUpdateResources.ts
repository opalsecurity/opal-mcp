/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesUpdateResources } from "../../funcs/resourcesUpdateResources.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.UpdateResourceInfoList$inboundSchema,
};

export const tool$resourcesUpdateResources: ToolDefinition<typeof args> = {
  name: "resources-update-resources",
  description: `Bulk updates a list of resources.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resourcesUpdateResources(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

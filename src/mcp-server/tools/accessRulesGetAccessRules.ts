/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accessRulesGetAccessRules } from "../../funcs/accessRulesGetAccessRules.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccessRulesRequest$inboundSchema,
};

export const tool$accessRulesGetAccessRules: ToolDefinition<typeof args> = {
  name: "access-rules-get-access-rules",
  description: `Returns a list of access rules for your organization.`,
  scopes: ["read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accessRulesGetAccessRules(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

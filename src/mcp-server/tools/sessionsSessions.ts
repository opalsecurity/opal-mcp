/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionsSessions } from "../../funcs/sessionsSessions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SessionsRequest$inboundSchema,
};

export const tool$sessionsSessions: ToolDefinition<typeof args> = {
  name: "sessions-sessions",
  description: `Returns a list of \`Session\` objects.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionsSessions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

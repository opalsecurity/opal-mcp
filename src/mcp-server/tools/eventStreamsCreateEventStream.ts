/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventStreamsCreateEventStream } from "../../funcs/eventStreamsCreateEventStream.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateEventStreamInfo$inboundSchema,
};

export const tool$eventStreamsCreateEventStream: ToolDefinition<typeof args> = {
  name: "event-streams-create-event-stream",
  description: `Create event stream

Creates a new event streaming connection.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await eventStreamsCreateEventStream(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { delegationsGetDelegations } from "../../funcs/delegationsGetDelegations.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetDelegationsRequest$inboundSchema,
};

export const tool$delegationsGetDelegations: ToolDefinition<typeof args> = {
  name: "delegations-get-delegations",
  description: `Get delegations

Returns a list of request reviewer delegations configured for your organization.`,
  scopes: ["read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await delegationsGetDelegations(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

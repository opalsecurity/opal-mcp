/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paladinGetPaladinFromName } from "../../funcs/paladinGetPaladinFromName.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPaladinFromNameRequest$inboundSchema,
};

export const tool$paladinGetPaladinFromName: ToolDefinition<typeof args> = {
  name: "paladin-get-paladin-from-name",
  description: `Get Paladins by name

Returns all Paladins whose name exactly matches the given name. Names are not unique, so the result is a list and may be empty.`,
  scopes: ["read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paladinGetPaladinFromName(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

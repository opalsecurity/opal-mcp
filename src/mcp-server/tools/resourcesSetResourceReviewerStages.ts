/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesSetResourceReviewerStages } from "../../funcs/resourcesSetResourceReviewerStages.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SetResourceReviewerStagesRequest$inboundSchema,
};

export const tool$resourcesSetResourceReviewerStages: ToolDefinition<
  typeof args
> = {
  name: "resources-set-resource-reviewer-stages",
  description: `Sets the list of reviewer stages for a resource.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resourcesSetResourceReviewerStages(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupsGetGroupAccessLevels } from "../../funcs/groupsGetGroupAccessLevels.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetGroupAccessLevelsRequest$inboundSchema,
};

export const tool$groupsGetGroupAccessLevels: ToolDefinition<typeof args> = {
  name: "groups-get-group-access-levels",
  description: `Get group access levels

Returns the list of access levels defined for the group. Groups that only offer default (unnamed) access return an empty list.`,
  scopes: ["read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await groupsGetGroupAccessLevels(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { configurationTemplatesGetConfigurationTemplates } from "../../funcs/configurationTemplatesGetConfigurationTemplates.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$configurationTemplatesGetConfigurationTemplates:
  ToolDefinition = {
    name: "configuration-templates-get-configuration-templates",
    description: `Returns a list of \`ConfigurationTemplate\` objects.`,
    scopes: ["read"],
    tool: async (client, ctx) => {
      const [result, apiCall] =
        await configurationTemplatesGetConfigurationTemplates(
          client,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };

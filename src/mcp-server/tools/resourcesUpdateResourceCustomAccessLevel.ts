/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesUpdateResourceCustomAccessLevel } from "../../funcs/resourcesUpdateResourceCustomAccessLevel.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateResourceCustomAccessLevelRequest$inboundSchema,
};

export const tool$resourcesUpdateResourceCustomAccessLevel: ToolDefinition<
  typeof args
> = {
  name: "resources-update-resource-custom-access-level",
  description:
    `Updates a custom access level identified by its remote ID. If the resource is a parent type, the update fans out to all child resources.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resourcesUpdateResourceCustomAccessLevel(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

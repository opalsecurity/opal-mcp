/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { campaignsStartCampaign } from "../../funcs/campaignsStartCampaign.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.StartCampaignRequest$inboundSchema,
};

export const tool$campaignsStartCampaign: ToolDefinition<typeof args> = {
  name: "campaigns-start-campaign",
  description: `Start campaign

Starts a draft campaign immediately, setting \`started_at\` and
\`started_by_user_id\`. Returns 400 if the campaign is not in draft
state, or if it is a recurring template (\`is_template: true\`) —
templates spawn draft campaigns on their schedule and cannot be
started directly.
`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await campaignsStartCampaign(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

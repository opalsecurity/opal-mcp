/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesGetResourceReviewerStages } from "../../funcs/resourcesGetResourceReviewerStages.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetResourceReviewerStagesRequest$inboundSchema,
};

export const tool$resourcesGetResourceReviewerStages: ToolDefinition<
  typeof args
> = {
  name: "resources-get-resource-reviewer-stages",
  description: `Gets the list reviewer stages for a resource.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resourcesGetResourceReviewerStages(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventStreamsUpdateEventStream } from "../../funcs/eventStreamsUpdateEventStream.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateEventStreamRequest$inboundSchema,
};

export const tool$eventStreamsUpdateEventStream: ToolDefinition<typeof args> = {
  name: "event-streams-update-event-stream",
  description: `Update event stream

Updates an existing event streaming connection.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await eventStreamsUpdateEventStream(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

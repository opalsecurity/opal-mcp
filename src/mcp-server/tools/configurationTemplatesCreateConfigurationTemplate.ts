/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { configurationTemplatesCreateConfigurationTemplate } from "../../funcs/configurationTemplatesCreateConfigurationTemplate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateConfigurationTemplateInfo$inboundSchema,
};

export const tool$configurationTemplatesCreateConfigurationTemplate:
  ToolDefinition<typeof args> = {
    name: "configuration-templates-create-configuration-template",
    description: `Creates a configuration template.`,
    scopes: ["write"],
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await configurationTemplatesCreateConfigurationTemplate(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesDeleteResourceNhi } from "../../funcs/resourcesDeleteResourceNhi.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteResourceNhiRequest$inboundSchema,
};

export const tool$resourcesDeleteResourceNhi: ToolDefinition<typeof args> = {
  name: "resources-delete-resource-nhi",
  description:
    `Removes a non-human identity's direct access from this resource.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resourcesDeleteResourceNhi(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

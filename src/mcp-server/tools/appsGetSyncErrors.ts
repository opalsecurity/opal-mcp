/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { appsGetSyncErrors } from "../../funcs/appsGetSyncErrors.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSyncErrorsRequest$inboundSchema,
};

export const tool$appsGetSyncErrors: ToolDefinition<typeof args> = {
  name: "apps-get-sync-errors",
  description:
    `Returns a list of recent sync errors that have occurred since the last successful sync.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await appsGetSyncErrors(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

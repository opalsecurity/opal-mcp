/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesGetResourceMessageChannels } from "../../funcs/resourcesGetResourceMessageChannels.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetResourceMessageChannelsRequest$inboundSchema,
};

export const tool$resourcesGetResourceMessageChannels: ToolDefinition<
  typeof args
> = {
  name: "resources-get-resource-message-channels",
  description:
    `Gets the list of audit message channels attached to a resource.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resourcesGetResourceMessageChannels(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

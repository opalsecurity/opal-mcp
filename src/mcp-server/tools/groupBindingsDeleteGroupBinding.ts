/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupBindingsDeleteGroupBinding } from "../../funcs/groupBindingsDeleteGroupBinding.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteGroupBindingRequest$inboundSchema,
};

export const tool$groupBindingsDeleteGroupBinding: ToolDefinition<typeof args> =
  {
    name: "group-bindings-delete-group-binding",
    description: `Deletes a group binding.`,
    scopes: ["write"],
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await groupBindingsDeleteGroupBinding(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };

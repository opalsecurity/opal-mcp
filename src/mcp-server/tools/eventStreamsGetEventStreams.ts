/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventStreamsGetEventStreams } from "../../funcs/eventStreamsGetEventStreams.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$eventStreamsGetEventStreams: ToolDefinition = {
  name: "event-streams-get-event-streams",
  description: `Get event streams

Returns a list of configured event streaming connections for your organization.`,
  scopes: ["read"],
  tool: async (client, ctx) => {
    const [result, apiCall] = await eventStreamsGetEventStreams(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

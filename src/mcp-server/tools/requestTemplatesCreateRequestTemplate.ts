/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { requestTemplatesCreateRequestTemplate } from "../../funcs/requestTemplatesCreateRequestTemplate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateRequestTemplateInfo$inboundSchema,
};

export const tool$requestTemplatesCreateRequestTemplate: ToolDefinition<
  typeof args
> = {
  name: "request-templates-create-request-template",
  description: `Creates a request template.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await requestTemplatesCreateRequestTemplate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

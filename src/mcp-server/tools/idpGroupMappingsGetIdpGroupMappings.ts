/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { idpGroupMappingsGetIdpGroupMappings } from "../../funcs/idpGroupMappingsGetIdpGroupMappings.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetIdpGroupMappingsRequest$inboundSchema,
};

export const tool$idpGroupMappingsGetIdpGroupMappings: ToolDefinition<
  typeof args
> = {
  name: "idp-group-mappings-get-idp-group-mappings",
  description:
    `Returns the configured set of available \`IdpGroupMapping\` objects for an Okta app.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await idpGroupMappingsGetIdpGroupMappings(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

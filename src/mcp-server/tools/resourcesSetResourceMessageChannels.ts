/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesSetResourceMessageChannels } from "../../funcs/resourcesSetResourceMessageChannels.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SetResourceMessageChannelsRequest$inboundSchema,
};

export const tool$resourcesSetResourceMessageChannels: ToolDefinition<
  typeof args
> = {
  name: "resources-set-resource-message-channels",
  description:
    `Sets the list of audit message channels attached to a resource.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resourcesSetResourceMessageChannels(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

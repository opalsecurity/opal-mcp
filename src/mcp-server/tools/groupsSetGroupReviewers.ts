/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupsSetGroupReviewers } from "../../funcs/groupsSetGroupReviewers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SetGroupReviewersRequest$inboundSchema,
};

export const tool$groupsSetGroupReviewers: ToolDefinition<typeof args> = {
  name: "groups-set-group-reviewers",
  description: `Sets the list of reviewers for a group.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await groupsSetGroupReviewers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

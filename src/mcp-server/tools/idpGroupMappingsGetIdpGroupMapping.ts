/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { idpGroupMappingsGetIdpGroupMapping } from "../../funcs/idpGroupMappingsGetIdpGroupMapping.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetIdpGroupMappingRequest$inboundSchema,
};

export const tool$idpGroupMappingsGetIdpGroupMapping: ToolDefinition<
  typeof args
> = {
  name: "idp-group-mappings-get-idp-group-mapping",
  description: `Gets an \`IdpGroupMapping\` object for an Okta app and group.`,
  scopes: ["read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await idpGroupMappingsGetIdpGroupMapping(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

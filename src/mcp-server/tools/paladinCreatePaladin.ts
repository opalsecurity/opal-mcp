/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paladinCreatePaladin } from "../../funcs/paladinCreatePaladin.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreatePaladinInfo$inboundSchema,
};

export const tool$paladinCreatePaladin: ToolDefinition<typeof args> = {
  name: "paladin-create-paladin",
  description: `Create Paladin

Creates a new \`Paladin\`.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paladinCreatePaladin(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { requestTemplatesGetRequestTemplates } from "../../funcs/requestTemplatesGetRequestTemplates.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$requestTemplatesGetRequestTemplates: ToolDefinition = {
  name: "request-templates-get-request-templates",
  description: `Returns a list of \`RequestTemplate\` objects.`,
  scopes: ["read"],
  tool: async (client, ctx) => {
    const [result, apiCall] = await requestTemplatesGetRequestTemplates(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

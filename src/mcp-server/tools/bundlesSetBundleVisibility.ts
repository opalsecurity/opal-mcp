/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { bundlesSetBundleVisibility } from "../../funcs/bundlesSetBundleVisibility.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SetBundleVisibilityRequest$inboundSchema,
};

export const tool$bundlesSetBundleVisibility: ToolDefinition<typeof args> = {
  name: "bundles-set-bundle-visibility",
  description: `Sets the visibility of the bundle.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await bundlesSetBundleVisibility(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

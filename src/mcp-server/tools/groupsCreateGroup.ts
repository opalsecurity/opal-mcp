/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupsCreateGroup } from "../../funcs/groupsCreateGroup.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateGroupInfo$inboundSchema,
};

export const tool$groupsCreateGroup: ToolDefinition<typeof args> = {
  name: "groups-create-group",
  description:
    `Creates an Opal group or [imports a remote group](https://docs.opal.dev/reference/end-system-objects).`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await groupsCreateGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

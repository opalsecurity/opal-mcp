/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesSetResourceVisibility } from "../../funcs/resourcesSetResourceVisibility.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SetResourceVisibilityRequest$inboundSchema,
};

export const tool$resourcesSetResourceVisibility: ToolDefinition<typeof args> =
  {
    name: "resources-set-resource-visibility",
    description: `Sets the visibility of this resource.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await resourcesSetResourceVisibility(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };

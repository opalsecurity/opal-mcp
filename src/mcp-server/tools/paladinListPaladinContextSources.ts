/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paladinListPaladinContextSources } from "../../funcs/paladinListPaladinContextSources.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListPaladinContextSourcesRequest$inboundSchema,
};

export const tool$paladinListPaladinContextSources: ToolDefinition<
  typeof args
> = {
  name: "paladin-list-paladin-context-sources",
  description: `List Paladin context sources

Returns the context sources (Slack channels and documents) configured for a Paladin.`,
  scopes: ["read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paladinListPaladinContextSources(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

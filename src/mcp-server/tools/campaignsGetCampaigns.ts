/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { campaignsGetCampaigns } from "../../funcs/campaignsGetCampaigns.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCampaignsRequest$inboundSchema,
};

export const tool$campaignsGetCampaigns: ToolDefinition<typeof args> = {
  name: "campaigns-get-campaigns",
  description: `Returns a list of \`Campaign\` objects.`,
  scopes: ["read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await campaignsGetCampaigns(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

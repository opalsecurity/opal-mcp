/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { requestsEscalateRequest } from "../../funcs/requestsEscalateRequest.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EscalateRequestRequest$inboundSchema,
};

export const tool$requestsEscalateRequest: ToolDefinition<typeof args> = {
  name: "requests-escalate-request",
  description: `Escalate request to skip-manager

Escalates a pending access request to the target user's skip-level manager.
Only the request's requester may call this endpoint.
`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await requestsEscalateRequest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

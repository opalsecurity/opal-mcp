/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { requestTemplatesDeleteRequestTemplate } from "../../funcs/requestTemplatesDeleteRequestTemplate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteRequestTemplateRequest$inboundSchema,
};

export const tool$requestTemplatesDeleteRequestTemplate: ToolDefinition<
  typeof args
> = {
  name: "request-templates-delete-request-template",
  description: `Deletes a request template.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await requestTemplatesDeleteRequestTemplate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupsUpdateGroupUser } from "../../funcs/groupsUpdateGroupUser.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateGroupUserRequest$inboundSchema,
};

export const tool$groupsUpdateGroupUser: ToolDefinition<typeof args> = {
  name: "groups-update-group-user",
  description: `Updates a user's access level or duration in this group.`,
  scopes: ["write"],
  annotations: {
    title: "Update Group User",
    readOnlyHint: false,
    destructiveHint: false,
    idempotentHint: true,
    openWorldHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await groupsUpdateGroupUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

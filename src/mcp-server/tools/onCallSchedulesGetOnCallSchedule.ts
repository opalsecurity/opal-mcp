/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { onCallSchedulesGetOnCallSchedule } from "../../funcs/onCallSchedulesGetOnCallSchedule.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOnCallScheduleRequest$inboundSchema,
};

export const tool$onCallSchedulesGetOnCallSchedule: ToolDefinition<
  typeof args
> = {
  name: "on-call-schedules-get-on-call-schedule",
  description: `Gets a \`OnCallSchedule\` object.`,
  scopes: ["read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await onCallSchedulesGetOnCallSchedule(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

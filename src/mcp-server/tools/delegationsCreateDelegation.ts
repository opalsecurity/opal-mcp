/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { delegationsCreateDelegation } from "../../funcs/delegationsCreateDelegation.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateDelegationRequest$inboundSchema,
};

export const tool$delegationsCreateDelegation: ToolDefinition<typeof args> = {
  name: "delegations-create-delegation",
  description:
    `Creates a new request reviewer delegation to delegate access review requests from one user to another.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await delegationsCreateDelegation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

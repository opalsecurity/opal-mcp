/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupsGetGroupMessageChannels } from "../../funcs/groupsGetGroupMessageChannels.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetGroupMessageChannelsRequest$inboundSchema,
};

export const tool$groupsGetGroupMessageChannels: ToolDefinition<typeof args> = {
  name: "groups-get-group-message-channels",
  description:
    `Gets the list of audit and reviewer message channels attached to a group.`,
  scopes: ["read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await groupsGetGroupMessageChannels(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { configurationTemplatesUpdateConfigurationTemplate } from "../../funcs/configurationTemplatesUpdateConfigurationTemplate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.UpdateConfigurationTemplateInfo$inboundSchema,
};

export const tool$configurationTemplatesUpdateConfigurationTemplate:
  ToolDefinition<typeof args> = {
    name: "configuration-templates-update-configuration-template",
    description: `Update a configuration template.`,
    scopes: ["write"],
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await configurationTemplatesUpdateConfigurationTemplate(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };

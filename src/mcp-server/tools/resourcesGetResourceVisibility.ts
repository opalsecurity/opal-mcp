/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesGetResourceVisibility } from "../../funcs/resourcesGetResourceVisibility.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetResourceVisibilityRequest$inboundSchema,
};

export const tool$resourcesGetResourceVisibility: ToolDefinition<typeof args> =
  {
    name: "resources-get-resource-visibility",
    description: `Gets the visibility of this resource.`,
    scopes: ["read"],
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await resourcesGetResourceVisibility(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };

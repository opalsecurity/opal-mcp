/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { requestsRemindRequestReviewer } from "../../funcs/requestsRemindRequestReviewer.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemindRequestReviewerRequest$inboundSchema,
};

export const tool$requestsRemindRequestReviewer: ToolDefinition<typeof args> = {
  name: "requests-remind-request-reviewer",
  description: `Send reminder to a reviewer

Sends a reminder to one pending reviewer of the request.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await requestsRemindRequestReviewer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

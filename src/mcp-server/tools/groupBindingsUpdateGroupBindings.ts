/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupBindingsUpdateGroupBindings } from "../../funcs/groupBindingsUpdateGroupBindings.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.UpdateGroupBindingInfoList$inboundSchema,
};

export const tool$groupBindingsUpdateGroupBindings: ToolDefinition<
  typeof args
> = {
  name: "group-bindings-update-group-bindings",
  description: `Bulk updates a list of group bindings.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await groupBindingsUpdateGroupBindings(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

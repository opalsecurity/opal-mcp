/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersGetUserWhoami } from "../../funcs/usersGetUserWhoami.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$usersGetUserWhoami: ToolDefinition = {
  name: "users-get-user-whoami",
  description: `Returns the user that the provided API token authenticates as.`,
  scopes: ["read"],
  tool: async (client, ctx) => {
    const [result, apiCall] = await usersGetUserWhoami(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

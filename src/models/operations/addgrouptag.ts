/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type AddGroupTagRequest = {
  /**
   * The ID of the tag to apply.
   */
  tagId: string;
  /**
   * The ID of the group to apply the tag to.
   */
  groupId: string;
};

/** @internal */
export const AddGroupTagRequest$inboundSchema: z.ZodType<
  AddGroupTagRequest,
  z.ZodTypeDef,
  unknown
> = z.object({
  tag_id: z.string(),
  group_id: z.string(),
}).transform((v) => {
  return remap$(v, {
    "tag_id": "tagId",
    "group_id": "groupId",
  });
});
/** @internal */
export type AddGroupTagRequest$Outbound = {
  tag_id: string;
  group_id: string;
};

/** @internal */
export const AddGroupTagRequest$outboundSchema: z.ZodType<
  AddGroupTagRequest$Outbound,
  z.ZodTypeDef,
  AddGroupTagRequest
> = z.object({
  tagId: z.string(),
  groupId: z.string(),
}).transform((v) => {
  return remap$(v, {
    tagId: "tag_id",
    groupId: "group_id",
  });
});

export function addGroupTagRequestToJSON(
  addGroupTagRequest: AddGroupTagRequest,
): string {
  return JSON.stringify(
    AddGroupTagRequest$outboundSchema.parse(addGroupTagRequest),
  );
}
export function addGroupTagRequestFromJSON(
  jsonString: string,
): SafeParseResult<AddGroupTagRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AddGroupTagRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AddGroupTagRequest' from JSON`,
  );
}

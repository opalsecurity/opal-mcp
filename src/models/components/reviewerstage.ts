/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  ReviewerStageEscalation,
  ReviewerStageEscalation$inboundSchema,
  ReviewerStageEscalation$Outbound,
  ReviewerStageEscalation$outboundSchema,
} from "./reviewerstageescalation.js";

/**
 * The operator of the reviewer stage. Admin and manager approval are also treated as reviewers. A stage that sets `escalation` must use `OR`; `AND` is rejected there, because the escalation timer joins the stage as an additional reviewer and would otherwise become a required approver that stalls every request until the timeout.
 */
export const Operator = {
  And: "AND",
  Or: "OR",
} as const;
/**
 * The operator of the reviewer stage. Admin and manager approval are also treated as reviewers. A stage that sets `escalation` must use `OR`; `AND` is rejected there, because the escalation timer joins the stage as an additional reviewer and would otherwise become a required approver that stalls every request until the timeout.
 */
export type Operator = ClosedEnum<typeof Operator>;

/**
 * A reviewer stage.
 */
export type ReviewerStage = {
  /**
   * Whether this reviewer stage should require manager approval.
   */
  requireManagerApproval: boolean;
  /**
   * Whether this reviewer stage should require admin approval.
   */
  requireAdminApproval?: boolean | undefined;
  /**
   * The operator of the reviewer stage. Admin and manager approval are also treated as reviewers. A stage that sets `escalation` must use `OR`; `AND` is rejected there, because the escalation timer joins the stage as an additional reviewer and would otherwise become a required approver that stalls every request until the timeout.
   */
  operator: Operator;
  /**
   * The IDs of owners assigned as reviewers for this stage.
   */
  ownerIds: Array<string>;
  /**
   * The IDs of service users assigned as reviewers for this stage.
   */
  serviceUserIds?: Array<string> | undefined;
  /**
   * Escalation for a reviewer stage. When set, the request advances to the
   *
   * @remarks
   * reviewers named here if nobody responds within delay_minutes. Timely
   * approval by any of the stage's own reviewers resolves the stage without
   * escalating.
   *
   * owner_ids and user_ids name only who to escalate to; the stage's own
   * reviewers are added automatically and must not be repeated here. A
   * stage with owner_ids [X] escalating to Y sets escalation.owner_ids to
   * [Y], and reviewing after escalation is then open to both X and Y.
   *
   * Because the stage's reviewers are unioned in rather than copied,
   * removing someone from the stage also removes them from the escalation.
   * At least one owner or user named here must not already be a reviewer
   * of the stage.
   */
  escalation?: ReviewerStageEscalation | undefined;
};

/** @internal */
export const Operator$inboundSchema: z.ZodNativeEnum<typeof Operator> = z
  .nativeEnum(Operator);
/** @internal */
export const Operator$outboundSchema: z.ZodNativeEnum<typeof Operator> =
  Operator$inboundSchema;

/** @internal */
export const ReviewerStage$inboundSchema: z.ZodType<
  ReviewerStage,
  z.ZodTypeDef,
  unknown
> = z.object({
  require_manager_approval: z.boolean(),
  require_admin_approval: z.boolean().optional(),
  operator: Operator$inboundSchema,
  owner_ids: z.array(z.string()),
  service_user_ids: z.array(z.string()).optional(),
  escalation: ReviewerStageEscalation$inboundSchema.optional(),
}).transform((v) => {
  return remap$(v, {
    "require_manager_approval": "requireManagerApproval",
    "require_admin_approval": "requireAdminApproval",
    "owner_ids": "ownerIds",
    "service_user_ids": "serviceUserIds",
  });
});
/** @internal */
export type ReviewerStage$Outbound = {
  require_manager_approval: boolean;
  require_admin_approval?: boolean | undefined;
  operator: string;
  owner_ids: Array<string>;
  service_user_ids?: Array<string> | undefined;
  escalation?: ReviewerStageEscalation$Outbound | undefined;
};

/** @internal */
export const ReviewerStage$outboundSchema: z.ZodType<
  ReviewerStage$Outbound,
  z.ZodTypeDef,
  ReviewerStage
> = z.object({
  requireManagerApproval: z.boolean(),
  requireAdminApproval: z.boolean().optional(),
  operator: Operator$outboundSchema,
  ownerIds: z.array(z.string()),
  serviceUserIds: z.array(z.string()).optional(),
  escalation: ReviewerStageEscalation$outboundSchema.optional(),
}).transform((v) => {
  return remap$(v, {
    requireManagerApproval: "require_manager_approval",
    requireAdminApproval: "require_admin_approval",
    ownerIds: "owner_ids",
    serviceUserIds: "service_user_ids",
  });
});

export function reviewerStageToJSON(reviewerStage: ReviewerStage): string {
  return JSON.stringify(ReviewerStage$outboundSchema.parse(reviewerStage));
}
export function reviewerStageFromJSON(
  jsonString: string,
): SafeParseResult<ReviewerStage, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReviewerStage$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReviewerStage' from JSON`,
  );
}

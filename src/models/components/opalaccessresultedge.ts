/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  OpalAccessResultNode,
  OpalAccessResultNode$inboundSchema,
  OpalAccessResultNode$Outbound,
  OpalAccessResultNode$outboundSchema,
} from "./opalaccessresultnode.js";

/**
 * A single ACCESS result edge containing the matched access grant and its pagination cursor.
 */
export type OpalAccessResultEdge = {
  /**
   * A matched access grant from an ACCESS OpalQuery — one per (principal, entitlement, access level), with every access path as metadata.
   */
  node: OpalAccessResultNode;
  /**
   * Opaque cursor for this grant, used for pagination.
   */
  cursor: string;
};

/** @internal */
export const OpalAccessResultEdge$inboundSchema: z.ZodType<
  OpalAccessResultEdge,
  z.ZodTypeDef,
  unknown
> = z.object({
  node: OpalAccessResultNode$inboundSchema,
  cursor: z.string(),
});
/** @internal */
export type OpalAccessResultEdge$Outbound = {
  node: OpalAccessResultNode$Outbound;
  cursor: string;
};

/** @internal */
export const OpalAccessResultEdge$outboundSchema: z.ZodType<
  OpalAccessResultEdge$Outbound,
  z.ZodTypeDef,
  OpalAccessResultEdge
> = z.object({
  node: OpalAccessResultNode$outboundSchema,
  cursor: z.string(),
});

export function opalAccessResultEdgeToJSON(
  opalAccessResultEdge: OpalAccessResultEdge,
): string {
  return JSON.stringify(
    OpalAccessResultEdge$outboundSchema.parse(opalAccessResultEdge),
  );
}
export function opalAccessResultEdgeFromJSON(
  jsonString: string,
): SafeParseResult<OpalAccessResultEdge, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OpalAccessResultEdge$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OpalAccessResultEdge' from JSON`,
  );
}

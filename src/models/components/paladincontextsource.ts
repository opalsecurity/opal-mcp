/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  PaladinContextSourceKind,
  PaladinContextSourceKind$inboundSchema,
  PaladinContextSourceKind$outboundSchema,
} from "./paladincontextsourcekind.js";
import {
  PaladinContextSourceProvider,
  PaladinContextSourceProvider$inboundSchema,
  PaladinContextSourceProvider$outboundSchema,
} from "./paladincontextsourceprovider.js";

/**
 * # PaladinContextSource Object
 *
 * @remarks
 * ### Description
 * A context source (a Slack channel or a document) that a Paladin is
 * configured to read during access-request review.
 */
export type PaladinContextSource = {
  /**
   * The ID of the context source.
   */
  id: string;
  /**
   * The ID of the Paladin this source belongs to.
   */
  paladinId: string;
  /**
   * The kind of source a Paladin can read. A SLACK_CHANNEL is a Slack channel; a DOCUMENT is a Notion or Confluence page.
   */
  sourceKind: PaladinContextSourceKind;
  /**
   * The integration a Paladin context source comes from.
   */
  thirdPartyProvider: PaladinContextSourceProvider;
  /**
   * The provider's identifier for the source. The Slack channel ID for a channel, or the Notion/Confluence page ID for a document.
   */
  remoteId: string;
  /**
   * A human-readable name for the source.
   */
  name: string;
  /**
   * A link to the source.
   */
  url: string;
};

/** @internal */
export const PaladinContextSource$inboundSchema: z.ZodType<
  PaladinContextSource,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  paladin_id: z.string(),
  source_kind: PaladinContextSourceKind$inboundSchema,
  third_party_provider: PaladinContextSourceProvider$inboundSchema,
  remote_id: z.string(),
  name: z.string(),
  url: z.string(),
}).transform((v) => {
  return remap$(v, {
    "paladin_id": "paladinId",
    "source_kind": "sourceKind",
    "third_party_provider": "thirdPartyProvider",
    "remote_id": "remoteId",
  });
});
/** @internal */
export type PaladinContextSource$Outbound = {
  id: string;
  paladin_id: string;
  source_kind: string;
  third_party_provider: string;
  remote_id: string;
  name: string;
  url: string;
};

/** @internal */
export const PaladinContextSource$outboundSchema: z.ZodType<
  PaladinContextSource$Outbound,
  z.ZodTypeDef,
  PaladinContextSource
> = z.object({
  id: z.string(),
  paladinId: z.string(),
  sourceKind: PaladinContextSourceKind$outboundSchema,
  thirdPartyProvider: PaladinContextSourceProvider$outboundSchema,
  remoteId: z.string(),
  name: z.string(),
  url: z.string(),
}).transform((v) => {
  return remap$(v, {
    paladinId: "paladin_id",
    sourceKind: "source_kind",
    thirdPartyProvider: "third_party_provider",
    remoteId: "remote_id",
  });
});

export function paladinContextSourceToJSON(
  paladinContextSource: PaladinContextSource,
): string {
  return JSON.stringify(
    PaladinContextSource$outboundSchema.parse(paladinContextSource),
  );
}
export function paladinContextSourceFromJSON(
  jsonString: string,
): SafeParseResult<PaladinContextSource, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PaladinContextSource$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PaladinContextSource' from JSON`,
  );
}

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * # Delegation Object
 *
 * @remarks
 * ### Description
 * The `Delegation` object represents a delegation of access review requests from one user to another.
 *
 * ### Usage Example
 * List from the `GET Delegations` endpoint.
 * Get from the `GET Delegation` endpoint.
 */
export type Delegation = {
  /**
   * The ID of the delegation.
   */
  id: string;
  /**
   * The ID of the user delegating their access review requests.
   */
  delegatorUserId: string;
  /**
   * The ID of the user being delegated to.
   */
  delegateUserId: string;
  /**
   * The start time of the delegation.
   */
  startTime: Date;
  /**
   * The end time of the delegation.
   */
  endTime: Date;
  /**
   * The reason for the delegation.
   */
  reason: string;
  /**
   * The creation time of the delegation.
   */
  createdAt: Date;
  /**
   * The last updated time of the delegation.
   */
  updatedAt: Date;
};

/** @internal */
export const Delegation$inboundSchema: z.ZodType<
  Delegation,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  delegator_user_id: z.string(),
  delegate_user_id: z.string(),
  start_time: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  end_time: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  reason: z.string(),
  created_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  updated_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
}).transform((v) => {
  return remap$(v, {
    "delegator_user_id": "delegatorUserId",
    "delegate_user_id": "delegateUserId",
    "start_time": "startTime",
    "end_time": "endTime",
    "created_at": "createdAt",
    "updated_at": "updatedAt",
  });
});
/** @internal */
export type Delegation$Outbound = {
  id: string;
  delegator_user_id: string;
  delegate_user_id: string;
  start_time: string;
  end_time: string;
  reason: string;
  created_at: string;
  updated_at: string;
};

/** @internal */
export const Delegation$outboundSchema: z.ZodType<
  Delegation$Outbound,
  z.ZodTypeDef,
  Delegation
> = z.object({
  id: z.string(),
  delegatorUserId: z.string(),
  delegateUserId: z.string(),
  startTime: z.date().transform(v => v.toISOString()),
  endTime: z.date().transform(v => v.toISOString()),
  reason: z.string(),
  createdAt: z.date().transform(v => v.toISOString()),
  updatedAt: z.date().transform(v => v.toISOString()),
}).transform((v) => {
  return remap$(v, {
    delegatorUserId: "delegator_user_id",
    delegateUserId: "delegate_user_id",
    startTime: "start_time",
    endTime: "end_time",
    createdAt: "created_at",
    updatedAt: "updated_at",
  });
});

export function delegationToJSON(delegation: Delegation): string {
  return JSON.stringify(Delegation$outboundSchema.parse(delegation));
}
export function delegationFromJSON(
  jsonString: string,
): SafeParseResult<Delegation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Delegation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Delegation' from JSON`,
  );
}

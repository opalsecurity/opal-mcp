/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  EventStreamConnection,
  EventStreamConnection$inboundSchema,
  EventStreamConnection$Outbound,
  EventStreamConnection$outboundSchema,
} from "./eventstreamconnection.js";

/**
 * An event streaming connection that publishes events to an external system.
 */
export type EventStream = {
  /**
   * The ID of the event stream.
   */
  eventStreamId: string;
  /**
   * The connection configuration for an event stream.
   */
  connection: EventStreamConnection;
};

/** @internal */
export const EventStream$inboundSchema: z.ZodType<
  EventStream,
  z.ZodTypeDef,
  unknown
> = z.object({
  event_stream_id: z.string(),
  connection: EventStreamConnection$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "event_stream_id": "eventStreamId",
  });
});
/** @internal */
export type EventStream$Outbound = {
  event_stream_id: string;
  connection: EventStreamConnection$Outbound;
};

/** @internal */
export const EventStream$outboundSchema: z.ZodType<
  EventStream$Outbound,
  z.ZodTypeDef,
  EventStream
> = z.object({
  eventStreamId: z.string(),
  connection: EventStreamConnection$outboundSchema,
}).transform((v) => {
  return remap$(v, {
    eventStreamId: "event_stream_id",
  });
});

export function eventStreamToJSON(eventStream: EventStream): string {
  return JSON.stringify(EventStream$outboundSchema.parse(eventStream));
}
export function eventStreamFromJSON(
  jsonString: string,
): SafeParseResult<EventStream, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EventStream$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EventStream' from JSON`,
  );
}

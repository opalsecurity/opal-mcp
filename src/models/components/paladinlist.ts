/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  Paladin,
  Paladin$inboundSchema,
  Paladin$Outbound,
  Paladin$outboundSchema,
} from "./paladin.js";

/**
 * # PaladinList Object
 *
 * @remarks
 * ### Description
 * A list of `Paladin` objects.
 */
export type PaladinList = {
  results: Array<Paladin>;
};

/** @internal */
export const PaladinList$inboundSchema: z.ZodType<
  PaladinList,
  z.ZodTypeDef,
  unknown
> = z.object({
  results: z.array(Paladin$inboundSchema),
});
/** @internal */
export type PaladinList$Outbound = {
  results: Array<Paladin$Outbound>;
};

/** @internal */
export const PaladinList$outboundSchema: z.ZodType<
  PaladinList$Outbound,
  z.ZodTypeDef,
  PaladinList
> = z.object({
  results: z.array(Paladin$outboundSchema),
});

export function paladinListToJSON(paladinList: PaladinList): string {
  return JSON.stringify(PaladinList$outboundSchema.parse(paladinList));
}
export function paladinListFromJSON(
  jsonString: string,
): SafeParseResult<PaladinList, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PaladinList$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PaladinList' from JSON`,
  );
}

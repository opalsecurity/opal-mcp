/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Escalation for a reviewer stage. When set, the request advances to the
 *
 * @remarks
 * reviewers named here if nobody responds within delay_minutes. Timely
 * approval by any of the stage's own reviewers resolves the stage without
 * escalating.
 *
 * owner_ids and user_ids name only who to escalate to; the stage's own
 * reviewers are added automatically and must not be repeated here. A
 * stage with owner_ids [X] escalating to Y sets escalation.owner_ids to
 * [Y], and reviewing after escalation is then open to both X and Y.
 *
 * Because the stage's reviewers are unioned in rather than copied,
 * removing someone from the stage also removes them from the escalation.
 * At least one owner or user named here must not already be a reviewer
 * of the stage.
 */
export type ReviewerStageEscalation = {
  /**
   * How long to wait for a response before escalating, in minutes. Between 1 and 1440 (24 hours).
   */
  delayMinutes: number;
  /**
   * The owners to escalate to. The stage's own owner_ids are added automatically and must not be repeated here.
   */
  ownerIds?: Array<string> | undefined;
  /**
   * The users to escalate to. The stage's own service_user_ids are added automatically and must not be repeated here.
   */
  userIds?: Array<string> | undefined;
};

/** @internal */
export const ReviewerStageEscalation$inboundSchema: z.ZodType<
  ReviewerStageEscalation,
  z.ZodTypeDef,
  unknown
> = z.object({
  delay_minutes: z.number().int(),
  owner_ids: z.array(z.string()).optional(),
  user_ids: z.array(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    "delay_minutes": "delayMinutes",
    "owner_ids": "ownerIds",
    "user_ids": "userIds",
  });
});
/** @internal */
export type ReviewerStageEscalation$Outbound = {
  delay_minutes: number;
  owner_ids?: Array<string> | undefined;
  user_ids?: Array<string> | undefined;
};

/** @internal */
export const ReviewerStageEscalation$outboundSchema: z.ZodType<
  ReviewerStageEscalation$Outbound,
  z.ZodTypeDef,
  ReviewerStageEscalation
> = z.object({
  delayMinutes: z.number().int(),
  ownerIds: z.array(z.string()).optional(),
  userIds: z.array(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    delayMinutes: "delay_minutes",
    ownerIds: "owner_ids",
    userIds: "user_ids",
  });
});

export function reviewerStageEscalationToJSON(
  reviewerStageEscalation: ReviewerStageEscalation,
): string {
  return JSON.stringify(
    ReviewerStageEscalation$outboundSchema.parse(reviewerStageEscalation),
  );
}
export function reviewerStageEscalationFromJSON(
  jsonString: string,
): SafeParseResult<ReviewerStageEscalation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReviewerStageEscalation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReviewerStageEscalation' from JSON`,
  );
}

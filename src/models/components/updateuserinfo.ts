/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The user fields to update.
 */
export type UpdateUserInfo = {
  /**
   * The user's manager ID. Set to null to remove the manager.
   */
  managerId?: string | null | undefined;
  /**
   * The user's position.
   */
  position?: string | undefined;
};

/** @internal */
export const UpdateUserInfo$inboundSchema: z.ZodType<
  UpdateUserInfo,
  z.ZodTypeDef,
  unknown
> = z.object({
  manager_id: z.nullable(z.string()).optional(),
  position: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "manager_id": "managerId",
  });
});
/** @internal */
export type UpdateUserInfo$Outbound = {
  manager_id?: string | null | undefined;
  position?: string | undefined;
};

/** @internal */
export const UpdateUserInfo$outboundSchema: z.ZodType<
  UpdateUserInfo$Outbound,
  z.ZodTypeDef,
  UpdateUserInfo
> = z.object({
  managerId: z.nullable(z.string()).optional(),
  position: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    managerId: "manager_id",
  });
});

export function updateUserInfoToJSON(updateUserInfo: UpdateUserInfo): string {
  return JSON.stringify(UpdateUserInfo$outboundSchema.parse(updateUserInfo));
}
export function updateUserInfoFromJSON(
  jsonString: string,
): SafeParseResult<UpdateUserInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UpdateUserInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UpdateUserInfo' from JSON`,
  );
}

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * # Access Level Object
 *
 * @remarks
 * ### Description
 * The `GroupAccessLevel` object is used to represent the level of access that a user has to a group or a group has to a group. The "default" access
 * level is a `GroupAccessLevel` object whose fields are all empty strings.
 *
 * ### Usage Example
 * View the `GroupAccessLevel` of a group/user or group/group pair to see the level of access granted to the group.
 */
export type GroupAccessLevel = {
  /**
   * The human-readable name of the access level.
   */
  accessLevelName: string;
  /**
   * The machine-readable identifier of the access level.
   */
  accessLevelRemoteId: string;
};

/** @internal */
export const GroupAccessLevel$inboundSchema: z.ZodType<
  GroupAccessLevel,
  z.ZodTypeDef,
  unknown
> = z.object({
  access_level_name: z.string(),
  access_level_remote_id: z.string(),
}).transform((v) => {
  return remap$(v, {
    "access_level_name": "accessLevelName",
    "access_level_remote_id": "accessLevelRemoteId",
  });
});

/** @internal */
export type GroupAccessLevel$Outbound = {
  access_level_name: string;
  access_level_remote_id: string;
};

/** @internal */
export const GroupAccessLevel$outboundSchema: z.ZodType<
  GroupAccessLevel$Outbound,
  z.ZodTypeDef,
  GroupAccessLevel
> = z.object({
  accessLevelName: z.string(),
  accessLevelRemoteId: z.string(),
}).transform((v) => {
  return remap$(v, {
    accessLevelName: "access_level_name",
    accessLevelRemoteId: "access_level_remote_id",
  });
});

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace GroupAccessLevel$ {
  /** @deprecated use `GroupAccessLevel$inboundSchema` instead. */
  export const inboundSchema = GroupAccessLevel$inboundSchema;
  /** @deprecated use `GroupAccessLevel$outboundSchema` instead. */
  export const outboundSchema = GroupAccessLevel$outboundSchema;
  /** @deprecated use `GroupAccessLevel$Outbound` instead. */
  export type Outbound = GroupAccessLevel$Outbound;
}

export function groupAccessLevelToJSON(
  groupAccessLevel: GroupAccessLevel,
): string {
  return JSON.stringify(
    GroupAccessLevel$outboundSchema.parse(groupAccessLevel),
  );
}

export function groupAccessLevelFromJSON(
  jsonString: string,
): SafeParseResult<GroupAccessLevel, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GroupAccessLevel$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GroupAccessLevel' from JSON`,
  );
}

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  PaladinConnector,
  PaladinConnector$inboundSchema,
  PaladinConnector$outboundSchema,
} from "./paladinconnector.js";

/**
 * Information for creating a Paladin.
 */
export type CreatePaladinInfo = {
  /**
   * The name of the Paladin.
   */
  name: string;
  /**
   * The ID of the owner of the Paladin.
   */
  ownerId: string;
  /**
   * When true, the Paladin reasons about requests but takes no action. Defaults to true.
   */
  monitorMode?: boolean | undefined;
  /**
   * When true, recommendations are visible only to admins. Only meaningful when monitor_mode is true. Defaults to false.
   */
  adminViewOnly?: boolean | undefined;
  /**
   * The connectors the Paladin is allowed to use.
   */
  enabledConnectors?: Array<PaladinConnector> | undefined;
  /**
   * The free-form instructions that guide the Paladin's decisions. Optional; if omitted the Paladin is created without instructions.
   */
  instructions?: string | undefined;
};

/** @internal */
export const CreatePaladinInfo$inboundSchema: z.ZodType<
  CreatePaladinInfo,
  z.ZodTypeDef,
  unknown
> = z.object({
  name: z.string(),
  owner_id: z.string(),
  monitor_mode: z.boolean().default(true),
  admin_view_only: z.boolean().default(false),
  enabled_connectors: z.array(PaladinConnector$inboundSchema).optional(),
  instructions: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "owner_id": "ownerId",
    "monitor_mode": "monitorMode",
    "admin_view_only": "adminViewOnly",
    "enabled_connectors": "enabledConnectors",
  });
});
/** @internal */
export type CreatePaladinInfo$Outbound = {
  name: string;
  owner_id: string;
  monitor_mode: boolean;
  admin_view_only: boolean;
  enabled_connectors?: Array<string> | undefined;
  instructions?: string | undefined;
};

/** @internal */
export const CreatePaladinInfo$outboundSchema: z.ZodType<
  CreatePaladinInfo$Outbound,
  z.ZodTypeDef,
  CreatePaladinInfo
> = z.object({
  name: z.string(),
  ownerId: z.string(),
  monitorMode: z.boolean().default(true),
  adminViewOnly: z.boolean().default(false),
  enabledConnectors: z.array(PaladinConnector$outboundSchema).optional(),
  instructions: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    ownerId: "owner_id",
    monitorMode: "monitor_mode",
    adminViewOnly: "admin_view_only",
    enabledConnectors: "enabled_connectors",
  });
});

export function createPaladinInfoToJSON(
  createPaladinInfo: CreatePaladinInfo,
): string {
  return JSON.stringify(
    CreatePaladinInfo$outboundSchema.parse(createPaladinInfo),
  );
}
export function createPaladinInfoFromJSON(
  jsonString: string,
): SafeParseResult<CreatePaladinInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreatePaladinInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreatePaladinInfo' from JSON`,
  );
}

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * An HMAC credential for webhook authentication.
 */
export type WebhookHmacCredential = {
  /**
   * The unique identifier for the credential.
   */
  id: string;
  /**
   * The HMAC secret value.
   */
  secret: string;
  /**
   * When the credential was created.
   */
  createdAt: Date;
};

/** @internal */
export const WebhookHmacCredential$inboundSchema: z.ZodType<
  WebhookHmacCredential,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  secret: z.string(),
  created_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
}).transform((v) => {
  return remap$(v, {
    "created_at": "createdAt",
  });
});
/** @internal */
export type WebhookHmacCredential$Outbound = {
  id: string;
  secret: string;
  created_at: string;
};

/** @internal */
export const WebhookHmacCredential$outboundSchema: z.ZodType<
  WebhookHmacCredential$Outbound,
  z.ZodTypeDef,
  WebhookHmacCredential
> = z.object({
  id: z.string(),
  secret: z.string(),
  createdAt: z.date().transform(v => v.toISOString()),
}).transform((v) => {
  return remap$(v, {
    createdAt: "created_at",
  });
});

export function webhookHmacCredentialToJSON(
  webhookHmacCredential: WebhookHmacCredential,
): string {
  return JSON.stringify(
    WebhookHmacCredential$outboundSchema.parse(webhookHmacCredential),
  );
}
export function webhookHmacCredentialFromJSON(
  jsonString: string,
): SafeParseResult<WebhookHmacCredential, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WebhookHmacCredential$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WebhookHmacCredential' from JSON`,
  );
}

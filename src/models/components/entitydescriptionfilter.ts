/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Filters GROUP and RESOURCE entities by a case-insensitive substring of their description ("contains"). USER entities have no description and never match, in either polarity. `not` inverts the match within the resource/group domain ("does NOT contain"), so it still returns only resources/groups rather than sweeping in users.
 *
 * @remarks
 */
export type EntityDescriptionFilter = {
  /**
   * The substring to match against the entity description.
   */
  string: string;
  /**
   * Invert the match — return resources/groups whose description does NOT contain the substring.
   */
  not?: boolean | undefined;
};

/** @internal */
export const EntityDescriptionFilter$inboundSchema: z.ZodType<
  EntityDescriptionFilter,
  z.ZodTypeDef,
  unknown
> = z.object({
  string: z.string(),
  not: z.boolean().optional(),
});
/** @internal */
export type EntityDescriptionFilter$Outbound = {
  string: string;
  not?: boolean | undefined;
};

/** @internal */
export const EntityDescriptionFilter$outboundSchema: z.ZodType<
  EntityDescriptionFilter$Outbound,
  z.ZodTypeDef,
  EntityDescriptionFilter
> = z.object({
  string: z.string(),
  not: z.boolean().optional(),
});

export function entityDescriptionFilterToJSON(
  entityDescriptionFilter: EntityDescriptionFilter,
): string {
  return JSON.stringify(
    EntityDescriptionFilter$outboundSchema.parse(entityDescriptionFilter),
  );
}
export function entityDescriptionFilterFromJSON(
  jsonString: string,
): SafeParseResult<EntityDescriptionFilter, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EntityDescriptionFilter$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EntityDescriptionFilter' from JSON`,
  );
}
